version: '3.8'

services:
  db:
    image: postgres:13.2-alpine
    container_name: nb_database
    ports:
      - '5432:5432'
    env_file:
      - .env

  redis:
    image: redis:7-alpine
    container_name: nb_redis
    ports:
      - '6379:6379'
